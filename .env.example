# docker-compose가 자동으로 읽는 환경변수 파일입니다.
# 사용법: cp .env.example .env  (그 다음 필요하면 값 수정)

# PostgreSQL — postgres 컨테이너 초기화 값 + app 컨테이너 접속 정보로 동일하게 사용됨
POSTGRES_DB=schedule
POSTGRES_USER=schedule
POSTGRES_PASSWORD=schedule
