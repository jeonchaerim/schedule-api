config.stopBubbling = true
# @RequiredArgsConstructor가 필드의 @Lazy를 생성자 파라미터까지 그대로 복사하게 함
# (RedissonClient를 지연 초기화하기 위해 필요 — ScheduleService 참고)
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
